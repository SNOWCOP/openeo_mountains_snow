"""
User-Defined Functions (UDFs) for the snow cover fraction pipeline.

These UDFs are loaded at runtime via ``openeo.UDF.from_file`` or
``importlib.resources`` and executed server-side on the openEO backend.
"""
